import 'package:flutter/material.dart';

class ComicsScreen extends StatelessWidget {
  const ComicsScreen({super.key});

  static const routeName = '/comics';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ComicsScreen'),
    );
    ();
  }
}
